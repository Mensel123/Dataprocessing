Explain the difference between the == operator and the === operator:
== is used when you whant to test whether a value has a real value
=== is used when you dont want any conversions and want to see whether two
values are precisely equal.

Explain what a closure is. (Note that JavaScript programs use closures very often):
A closure is a function that can acces the value inside its parent function.
You can call this function and then the value changes ones but you can still
use the child function. ???

Explain what higher order functions are:
Higher order functions use other functions.

Explain what a query selector is and give an example line of JavaScript that uses a query selector:
querySelector() returns the first object that matches the css element you gave it
document.querySelector("div.w3-container top")
